<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digipodium.lkobus">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NearByStop"
            android:label="NearByStop" />
        <activity
            android:name=".AlertActivity"
            android:label="AlertActivity" />
        <activity
            android:name=".FareDetails"
            android:label="FareDetails" />
        <activity
            android:name=".RouteSearch"
            android:label="ROUTE SEARCH" />
        <activity
            android:name=".Route_search_second"
            android:label="@string/title_activity_route_search_second"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".IconActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RoutePlanActivity" />
        <activity
            android:name=".AboutUsActivity"
            android:label="@string/title_activity_about_us" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".RouteMapActivity"
            android:label="@string/title_activity_route_map" />
        <activity
            android:name=".TraceBusMapActivity"
            android:label="@string/title_activity_trace_bus_map" />
        <activity
            android:name=".RouteSearchMapActivity"
            android:label="@string/title_activity_route_search_map"></activity>
    </application>

</manifest>